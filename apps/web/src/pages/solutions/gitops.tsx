import React from "react"
import * as styles from "./gitops.module.css"

/* eslint-disable-next-line */
export interface GitopsProps { }

export function Gitops(props: GitopsProps) {
  return (
    <div>
      <h1>Welcome to Gitops!</h1>
    </div>
  )
}

export default Gitops
